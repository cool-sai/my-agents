# 第 14 课：取消正在跑的一轮

## 1. 关掉页面不够

刷新会把原来的 `EventSource` 关掉。浏览器不听了，但这一轮不能跟着停。它在页面外面接着跑。

再打开时，`/history` 马上返回：已经落库的句子，加上这一轮内存里已经吐出来的字。不等这篇文章写完。`/listen` 只接后面的新字。模型还在写的这些字不在 MySQL 里，只在这一轮的内存里。

另一段会话也不用等。连接只在一次读取或一个节点写完时占着，不会被整篇文章握住。切过去，历史马上出来；那边再发一句，也是自己的一轮。

这一轮跑完后再刷新，历史里就是完整的。进程要是退出了，这一轮会停，页面再刷新也读不到。那是服务没在听。

只把按钮变回「发送」，或者只关掉连接，都不是取消。取消是下面那面旗。

## 2. 一面旗

```text
点停止
  ↓
GET /cancel?thread_id=这一段
  ↓
这一轮的 RunControl.request_drain()
  ↓
slow_search 每 0.1 秒看一次旗，看到就返回「已取消，没有查完」
  ↓
图在下一次模型调用之前停下
  ↓
页面收到 cancelled，不再追加 token
```

旗是 `RunControl`。`/cancel` 和正在跑的工具看的是同一个对象，所以另一个请求能把这一轮喊停。

工具要是一次睡满 6 秒、中间不看旗，点了停止也要等它睡完。慢查询因此是一小段一小段睡。

模型那一次已经发出的 HTTP 不会从网上拽回来。停的是：工具不再做完，以及做完工具之后不再接着生成下一段回答。

## 3. 为什么要补一条工具结果

取消若发生在「模型已经决定调工具、工具还没返回」，快照里会留下一个没有 `ToolMessage` 的 tool_call。下一轮把这串消息送回模型，接口会拒。

所以旗放下之后，缺结果的调用要补上同一句「已取消，没有查完」。下一句用户消息是新的一轮：没走完的模型步骤被丢掉，不会把取消前的后半段接着吐出来。

用户这句话在开跑之前就写入快照。模型还在想、或者慢查询还在睡的时候刷新，页面重新读 `/history`，这句话还在。以前要等这一步跑完才落库，刷新读到的是上一个快照，刚发出的句子就没了。

## 4. 跑起来

不要和 `10_web.py`、`13_session.py` 一起开。三个都占 `8765`，只有这一课有 `/cancel`。

```bash
python 14_cancel.py
cd 10_web && npm run dev
```

浏览器打开 http://127.0.0.1:5173 。

1. 发送「慢查询：会员规则」。
2. 工具卡片还在、回答还没出来时点停止。
3. 页面不再往下蹦字。工具结果是「已取消，没有查完」，不是「慢查询结果」。
4. 再发一句不含「慢查询」的话。这句是新的一轮，不会把刚才没写完的回答补完。
