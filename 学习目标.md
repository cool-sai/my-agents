学习目标

完成这套学习后，你应该能够独立完成以下工作。
当前课按 1–12 走；13 之后是远期，先记目标，先不写代码。

当前阶段：LangChain（先原生，再框架）

1. 使用统一接口调用不同厂商的大模型。
2. 理解并处理 SystemMessage、HumanMessage、AIMessage 和 ToolMessage。
3. 为模型定义和绑定工具。
4. 不依赖框架，手写一个 Tool Calling 循环。
5. 使用 create_agent 创建完整 Agent。
6. 使用 Pydantic 约束模型的结构化输出。
7. 为 Agent 增加动态提示词、中间件、错误处理和权限控制。
8. 实现流式输出，并通过 FastAPI 和 SSE 发送给前端。
9. 实现短期会话记忆。
10. 构建基础 RAG 和 Agentic RAG。
11. 对 Agent 进行日志追踪、测试和评估。
12. 将 LangChain Agent 接入 React 应用（Vite，页面只收 SSE）。

远期：对照 SDK

13. 用 OpenAI Agents SDK 把同一套事情再走一遍：工具、循环、会话、流式、guardrail。
    对照 LangChain：哪些只是换了名字，哪些是不同的编排模型。

远期：全面框架

14. 读 nanobot 这类完整 Agent 运行时（通道、技能、记忆、provider、权限）。
15. 自己写一个小型但完整的框架：能接模型、装技能、跑多轮、接到一种会话入口。

远期：Coding Agent

16. 读 Claude Code、Codex 源码，拆出 harness、工具（shell / 文件）、权限、上下文压缩、循环。
17. 仿写一个小型 coding agent：能读改本地文件、跑命令，多轮改完一个小任务。
