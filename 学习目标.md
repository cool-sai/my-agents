# 学习目标

目标：能独立做出一个经得起失败的 Agent 产品，接近工作 8–10 年的 agent 全栈工程师。
不是再多熟悉一个框架的 API。

仍然是这一个产品往下做（星林 Agent：Python 里的循环 + Vite 页面收 SSE）。不要为了下一课换一套框架。

## 已完成：基础（1–12）

1. 使用统一接口调用不同厂商的大模型。
2. 理解并处理 SystemMessage、HumanMessage、AIMessage 和 ToolMessage。
3. 为模型定义和绑定工具。
4. 不依赖框架，手写一个 Tool Calling 循环。
5. 使用 create_agent 创建完整 Agent。
6. 使用 Pydantic 约束模型的结构化输出。
7. 为 Agent 增加动态提示词、中间件、错误处理和权限控制。
8. 实现流式输出，并通过 FastAPI 和 SSE 发送给前端。
9. 实现短期会话记忆。
10. 构建基础 RAG 和 Agentic RAG。
11. 对 Agent 进行日志追踪、测试和评估。
12. 将 LangChain Agent 接入 React 应用（Vite，页面只收 SSE）。

这 12 条证明一件事：模型只提议调用，应用负责执行并把结果送回去，页面只把事件画出来。

## 接下来：把这一个产品做扎实

从 13 接着学。一条做完再下一条。

13. 把第 06 课的会话接到页面上。
    刷新之后还在，第二轮记得上一轮。现在的页面每次都是一次新调用。
    算过：关掉标签再打开，同一条会话还在；追问「刚才那个城市」不用把城市再打一遍。

14. 能取消正在跑的一轮。
    停掉流，也停掉还没做完的工具。只把按钮变回「发送」不算过。
    算过：工具还在跑时点取消，这一轮不再往页面上追加 token，下一轮是新的一次。

15. 换一个会失败的真工具。
    超时、参数错、结果要显示在页面上。有副作用的动作先等人点头再执行。
    算过：故意让工具失败时，页面上能看到失败，而不是一直转圈；没点确认，副作用不会发生。

16. 改提示词就重跑一组固定题。
    用第 09 课的判法：该调的工具调了没有，关键事实在不在。绑到这个应用上，不要手跑三个例子。
    算过：一条命令跑完这组题；提示词改坏时，命令以失败退出。

17. 上下文装不下的时候怎么办。
    文档要切片、回答要带上出处；手册里没有就说没有。对话太长要压缩，而不是无限往 messages 里塞。
    算过：一篇放不进一次提示词的材料，仍能答到其中一处并指出出处；把会话聊很长之后，旧事实还在，请求不会因为超长直接失败。

18. 只读一个真正的 harness。
    Claude Code 或 Codex 里挑一个。看权限、沙箱、上下文压缩、循环。不要两个都通读。
    算过：能用自己的话指出它的循环和 01 的 while 哪里一样，权限和压缩是它多出来的哪两层。

19. 仿一个小的 coding agent。这是验收。
    能读改本地文件、跑命令，多轮做完一个小任务。
    算过：给一个很小的代码任务，它自己改完并跑通，不需要你把改哪一行写进提示词。

## 先不放进主线

- 自己写一个通用框架。等 19 做完，真的觉得缺一层再写。
- 多 Agent 编排。多数时候是把一个循环拆碎。
- 把另一个 SDK 整个再学一遍。名字对照可以以后用一晚做完，不单开一阶段。
