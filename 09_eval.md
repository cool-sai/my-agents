# 第 09 课：轨迹、测试、评估

## 1. 08 之后还缺什么

Agent 跑完，终端里是一段答案。看不出三件事：

- 它有没有查手册
- 查的是不是这道题
- 答案里的「200」「30 天」是手册里的，还是编的

这不是新的循环。还是 08 那个 `agent.invoke` 返回的 `messages`，换三种读法。

```text
messages
  ├─ trace：按时间记下来
  ├─ test：不调模型，用固定输入 assert
  └─ eval：调模型，只查工具和关键事实
```

LangSmith 是把 trace 发到网站上看。本课先在本地把这三层跑通。没有 trace，测试和评估没有东西可查。

## 2. 三个实验

```bash
python 09_eval.py test
python 09_eval.py trace
python 09_eval.py eval
```

建议先跑 `test`。它不调模型，用来看评分器认什么。再跑 `trace`，看一次真实调用长什么样。最后跑 `eval`。

手册和 `search_handbook` 直接复用第 08 课，不另写一份。

### trace：把 messages 读成轨迹

`agent.invoke` 的返回值里，`result["messages"]` 就是这次循环的全部消息。按顺序读：

```text
user: 银卡怎么升级？退卡的钱怎么退？
model → search_handbook({"query": "..."})
tool search_handbook ← 星辉卡消费满 200 元...
model: 消费满 200 元升银卡，余额 30 天内原路返回。
```

这就是日志。02 课已经在打印中间步骤，这里只是收成固定格式，后面的评分只看这个格式。

### test：不调模型

两件能钉死的事：

1. `retrieve`。已知问题必须命中会员那条；「上市了吗」必须是空。这和模型无关，08 的打分函数是确定的。
2. 评分器。造两段假的 `messages`：
   - 有 `tool_calls`，最终回答里有 `200` 和 `30` → 必须通过
   - 模型直接编「大概 100 元」，没有工具 → 必须失败

失败的那条不是程序坏了。评分器指出失败，测试才算通过。

### eval：调模型，不比整段原文

模型每次用词不一样，不能 `assert answer == "消费满 200 元..."`。每道题只查两条：

| 检查 | 含义 |
|---|---|
| 调用了 `search_handbook` | 答案来自工具，不是空口说的 |
| 回答里有指定的几个词 | `200`、`30`、`坚果`、`没有` |

三道题：

- 银卡怎么升级、退卡怎么退 → 要有 `200` 和 `30`
- 燕麦奶会过敏吗 → 要有 `坚果`
- 星林咖啡上市了吗 → 要有 `没有`

某一题 FAIL，先看轨迹里有没有 `model → search_handbook`。没有，就是模型没遵守「先查手册」。有工具、回答仍缺数字，就是它没照工具结果说。这两类修法不一样。

`eval` 会真的调模型，所以可能因为模型没听话而退出码为 1。`test` 不该失败。

## 3. 对照

| | test | eval |
|---|---|---|
| 调不调模型 | 不调 | 调 |
| 输入 | 写死的问题和假 messages | 真实 `agent.invoke` |
| 失败说明 | 检索或评分器写错了 | 模型没调工具，或没把事实说出来 |
| 能不能要求逐字相同 | 假消息可以 | 不能，只查关键词 |

## 4. 这一课的核心

```text
trace = 按时间读 messages
test  = 不调模型，钉死检索和评分器
eval  = 调模型，只查「工具调了没有、关键事实在不在」
```

答案文本本身不是证据。证据是轨迹里有没有那一次工具调用，以及工具返回的原文里有没有这个数。
